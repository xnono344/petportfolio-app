import 'react-native-url-polyfill/auto';

// Keep Expo Router's entry import last so it can register the app after all
// global polyfills and service initialization have run.
import 'expo-router/entry';
